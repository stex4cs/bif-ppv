═══════════════════════════════════════════════════════════════
   🚀 GITHUB AUTO-DEPLOY - QUICK START (5 minuta!)
═══════════════════════════════════════════════════════════════

📋 BRZI SETUP:
═══════════════════════════════════════════════════════════════

1️⃣ KREIRAJ GITHUB REPO
───────────────────────────────────────────────────────────────
   GitHub.com → New Repository → "bif-ppv"


2️⃣ PUSH KOD NA GITHUB
───────────────────────────────────────────────────────────────
   cd c:\xampp\htdocs\bif-PPV
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/USERNAME/bif-ppv.git
   git branch -M main
   git push -u origin main


3️⃣ DODAJ FTP SECRETS
───────────────────────────────────────────────────────────────
   GitHub → Repo → Settings → Secrets → Actions

   Dodaj 3 секрета:

   Secret #1:
   ┌────────────────────────────┐
   │ Name:  FTP_SERVER          │
   │ Value: ftp.bif.events      │
   │ (ili srv962-files.hstgr.io)│
   └────────────────────────────┘

   Secret #2:
   ┌────────────────────────────┐
   │ Name:  FTP_USERNAME        │
   │ Value: tvoj_ftp_username   │
   └────────────────────────────┘

   Secret #3:
   ┌────────────────────────────┐
   │ Name:  FTP_PASSWORD        │
   │ Value: tvoj_ftp_password   │
   └────────────────────────────┘


4️⃣ KREIRAJ env/.env NA SERVERU (MANUAL - Jednom!)
───────────────────────────────────────────────────────────────
   Hostinger cPanel → File Manager → public_html/env/

   Kreiraj fajl: .env
   Copy iz: DEPLOYMENT_SIMPLE.md (sekcija 3)
   PROMENI SVE LOZINKE!

   ili

   FileZilla → Upload local env/.env (sa NOVIM lozinkama!)


5️⃣ TEST DEPLOY
───────────────────────────────────────────────────────────────
   # Napravi test izmenu
   echo "Test" >> test.txt
   git add test.txt
   git commit -m "Test auto-deploy"
   git push origin main

   # Proveri GitHub Actions
   GitHub → Actions → "Deploy to Hostinger" job
   → Trebalo bi zelena kvačica ✅


6️⃣ VERIFY
───────────────────────────────────────────────────────────────
   ✅ https://bif.events → Sajt radi
   ✅ curl -I https://bif.events/env/.env → 403 Forbidden
   ✅ GitHub Actions → Green checkmark

═══════════════════════════════════════════════════════════════

✨ GOTOVO! Sada svaki put kada push-uješ na GitHub:
═══════════════════════════════════════════════════════════════

   git add .
   git commit -m "Opis izmena"
   git push origin main

   → GitHub automatski deploy-uje na https://bif.events! 🚀

═══════════════════════════════════════════════════════════════

📖 DETALJNI GUIDE:
═══════════════════════════════════════════════════════════════
   → GITHUB_DEPLOY_SETUP.md

═══════════════════════════════════════════════════════════════

⚠️ VAŽNO:
═══════════════════════════════════════════════════════════════

   ❌ NIKAD ne commit-uj env/.env na GitHub
      (već je u .gitignore)

   ✅ env/.env kreiraj MANUAL na serveru (jednom)

   ✅ .htaccess automatski blokira pristup env/ (403)

   ✅ Automatski deploy SAMO na main branch

═══════════════════════════════════════════════════════════════

🔧 HOSTINGER FTP CREDENTIALS:
═══════════════════════════════════════════════════════════════
   Hostinger cPanel → File Manager ili FTP Accounts
   → FTP/SFTP Details

   Trebaće ti:
   - Server address (ftp.bif.events)
   - Username
   - Password
   - Port (obično 21)

═══════════════════════════════════════════════════════════════

💡 TIPS:
═══════════════════════════════════════════════════════════════

   Dev Branch (opciono):
   ────────────────────
   git checkout -b dev
   git push origin dev
   → Deploy-uje se na staging.bif.events

   Manual Deploy:
   ────────────────────
   GitHub → Actions → Deploy to Hostinger
   → Run workflow → Run workflow button

   Rollback:
   ────────────────────
   git log (nađi commit hash)
   git revert <hash>
   git push origin main

═══════════════════════════════════════════════════════════════
